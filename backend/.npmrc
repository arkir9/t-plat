# Faster installs: skip audit/fund, prefer cache
prefer-offline=true
audit=false
fund=false
progress=true
no-optional=false
# Shorter timeout for hanging registry (ms)
fetch-timeout=60000
fetch-retries=2
maxsockets=4
# Skip scripts during install (optional; remove if you need postinstall)
ignore-scripts=false
